-- WoW Team Hub v6.0 - Ultra Modern UI
-- Design inspiré de: Glassmorphism, Neumorphism & Material Design 3

local Library = {}
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

-- Configuration des couleurs modernes
local Theme = {
    Primary = Color3.fromRGB(139, 92, 246), -- Purple vibrant
    Secondary = Color3.fromRGB(236, 72, 153), -- Pink moderne
    Accent = Color3.fromRGB(34, 211, 238), -- Cyan électrique
    Success = Color3.fromRGB(16, 185, 129), -- Green moderne
    
    Background = Color3.fromRGB(10, 10, 15), -- Très sombre
    Surface = Color3.fromRGB(17, 17, 24), -- Surface sombre
    SurfaceLight = Color3.fromRGB(24, 24, 32), -- Surface claire
    
    Text = Color3.fromRGB(240, 240, 245),
    TextSecondary = Color3.fromRGB(156, 163, 175),
    TextMuted = Color3.fromRGB(100, 100, 120),
    
    Border = Color3.fromRGB(40, 40, 55),
    Hover = Color3.fromRGB(30, 30, 42),
}

-- Icônes modernes
local Icons = {
    Home = "rbxassetid://10734950309",
    Settings = "rbxassetid://10734950309",
    User = "rbxassetid://10747372992",
    Search = "rbxassetid://10734898355",
    Plus = "rbxassetid://10747372992",
    Check = "rbxassetid://10734896629",
    X = "rbxassetid://10747384394",
    ChevronDown = "rbxassetid://10734896206",
}

-- Utilitaires d'animation
local function Tween(obj, props, duration, style, direction)
    duration = duration or 0.3
    style = style or Enum.EasingStyle.Quint
    direction = direction or Enum.EasingDirection.Out
    
    local tween = TweenService:Create(
        obj,
        TweenInfo.new(duration, style, direction),
        props
    )
    tween:Play()
    return tween
end

local function Spring(obj, props, duration)
    return Tween(obj, props, duration or 0.5, Enum.EasingStyle.Elastic, Enum.EasingDirection.Out)
end

-- Effet de particules (optionnel)
local function CreateGlow(parent, size, color)
    local glow = Instance.new("ImageLabel")
    glow.Name = "Glow"
    glow.Parent = parent
    glow.BackgroundTransparency = 1
    glow.Position = UDim2.new(0.5, 0, 0.5, 0)
    glow.AnchorPoint = Vector2.new(0.5, 0.5)
    glow.Size = size or UDim2.new(1.5, 0, 1.5, 0)
    glow.ZIndex = 0
    glow.Image = "rbxassetid://5028857084"
    glow.ImageColor3 = color or Theme.Primary
    glow.ImageTransparency = 0.7
    return glow
end

-- Fonction de dragging améliorée
local function MakeDraggable(frame, dragHandle)
    local dragging, dragInput, dragStart, startPos
    local velocity = Vector2.new(0, 0)
    
    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            
            Tween(frame, {Size = frame.Size + UDim2.new(0, 4, 0, 4)}, 0.1)
        end
    end)
    
    dragHandle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            velocity = delta
            frame.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
    
    dragHandle.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
            Tween(frame, {Size = frame.Size - UDim2.new(0, 4, 0, 4)}, 0.2)
        end
    end)
end

-- Fonction principale
function Library:CreateWindow(title)
    local WindowManager = {}
    WindowManager.Tabs = {}
    WindowManager.CurrentTab = nil
    
    -- ScreenGui
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "WoWTeamHubV6"
    ScreenGui.Parent = game.CoreGui
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.ResetOnSpawn = false
    
    -- Blur effet de fond (optionnel)
    local BlurEffect = Instance.new("BlurEffect")
    BlurEffect.Size = 0
    BlurEffect.Parent = game:GetService("Lighting")
    
    -- Container principal avec glassmorphism
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = Theme.Background
    MainFrame.BackgroundTransparency = 0.05
    MainFrame.BorderSizePixel = 0
    MainFrame.Position = UDim2.new(0.5, -300, 0.5, -225)
    MainFrame.Size = UDim2.new(0, 0, 0, 0)
    MainFrame.ClipsDescendants = true
    
    -- Animation d'ouverture
    Spring(MainFrame, {Size = UDim2.new(0, 600, 0, 450)}, 0.6)
    Tween(BlurEffect, {Size = 10}, 0.4)
    
    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 20)
    MainCorner.Parent = MainFrame
    
    -- Bordure lumineuse animée
    local BorderGradient = Instance.new("UIStroke")
    BorderGradient.Parent = MainFrame
    BorderGradient.Color = Theme.Primary
    BorderGradient.Thickness = 2
    BorderGradient.Transparency = 0.5
    BorderGradient.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    
    -- Animation de la bordure
    spawn(function()
        while MainFrame.Parent do
            Tween(BorderGradient, {Color = Theme.Secondary}, 2)
            wait(2)
            Tween(BorderGradient, {Color = Theme.Accent}, 2)
            wait(2)
            Tween(BorderGradient, {Color = Theme.Primary}, 2)
            wait(2)
        end
    end)
    
    -- Effet de profondeur
    local Shadow = Instance.new("ImageLabel")
    Shadow.Name = "Shadow"
    Shadow.Parent = MainFrame
    Shadow.BackgroundTransparency = 1
    Shadow.Position = UDim2.new(0, -20, 0, -20)
    Shadow.Size = UDim2.new(1, 40, 1, 40)
    Shadow.ZIndex = 0
    Shadow.Image = "rbxassetid://5028857084"
    Shadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    Shadow.ImageTransparency = 0.3
    Shadow.ScaleType = Enum.ScaleType.Slice
    Shadow.SliceCenter = Rect.new(24, 24, 276, 276)
    
    -- Barre supérieure moderne
    local TopBar = Instance.new("Frame")
    TopBar.Name = "TopBar"
    TopBar.Parent = MainFrame
    TopBar.BackgroundColor3 = Theme.Surface
    TopBar.BackgroundTransparency = 0.3
    TopBar.BorderSizePixel = 0
    TopBar.Size = UDim2.new(1, 0, 0, 60)
    
    local TopBarCorner = Instance.new("UICorner")
    TopBarCorner.CornerRadius = UDim.new(0, 20)
    TopBarCorner.Parent = TopBar
    
    -- Gradient subtil
    local TopBarGradient = Instance.new("UIGradient")
    TopBarGradient.Parent = TopBar
    TopBarGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Theme.Surface),
        ColorSequenceKeypoint.new(1, Theme.SurfaceLight)
    }
    TopBarGradient.Rotation = 90
    
    -- Logo/Icône animée
    local LogoContainer = Instance.new("Frame")
    LogoContainer.Name = "LogoContainer"
    LogoContainer.Parent = TopBar
    LogoContainer.BackgroundColor3 = Theme.Primary
    LogoContainer.BorderSizePixel = 0
    LogoContainer.Position = UDim2.new(0, 15, 0.5, -18)
    LogoContainer.Size = UDim2.new(0, 36, 0, 36)
    
    local LogoCorner = Instance.new("UICorner")
    LogoCorner.CornerRadius = UDim.new(0, 10)
    LogoCorner.Parent = LogoContainer
    
    local LogoGradient = Instance.new("UIGradient")
    LogoGradient.Parent = LogoContainer
    LogoGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Theme.Primary),
        ColorSequenceKeypoint.new(1, Theme.Secondary)
    }
    LogoGradient.Rotation = 45
    
    -- Animation rotation du logo
    spawn(function()
        while LogoContainer.Parent do
            Tween(LogoGradient, {Rotation = 405}, 3, Enum.EasingStyle.Linear)
            wait(3)
            LogoGradient.Rotation = 45
        end
    end)
    
    local LogoText = Instance.new("TextLabel")
    LogoText.Parent = LogoContainer
    LogoText.BackgroundTransparency = 1
    LogoText.Size = UDim2.new(1, 0, 1, 0)
    LogoText.Font = Enum.Font.GothamBold
    LogoText.Text = "W"
    LogoText.TextColor3 = Color3.fromRGB(255, 255, 255)
    LogoText.TextSize = 20
    
    -- Titre avec effet
    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Name = "Title"
    TitleLabel.Parent = TopBar
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Position = UDim2.new(0, 60, 0, 12)
    TitleLabel.Size = UDim2.new(0, 300, 0, 24)
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.Text = title or "WoW Team Hub"
    TitleLabel.TextColor3 = Theme.Text
    TitleLabel.TextSize = 18
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
    
    local TitleGradient = Instance.new("UIGradient")
    TitleGradient.Parent = TitleLabel
    TitleGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Theme.Text),
        ColorSequenceKeypoint.new(0.5, Theme.Primary),
        ColorSequenceKeypoint.new(1, Theme.Text)
    }
    
    -- Version badge
    local VersionBadge = Instance.new("Frame")
    VersionBadge.Parent = TopBar
    VersionBadge.BackgroundColor3 = Theme.Primary
    VersionBadge.BorderSizePixel = 0
    VersionBadge.Position = UDim2.new(0, 60, 0, 38)
    VersionBadge.Size = UDim2.new(0, 45, 0, 16)
    
    local BadgeCorner = Instance.new("UICorner")
    BadgeCorner.CornerRadius = UDim.new(0, 8)
    BadgeCorner.Parent = VersionBadge
    
    local VersionLabel = Instance.new("TextLabel")
    VersionLabel.Parent = VersionBadge
    VersionLabel.BackgroundTransparency = 1
    VersionLabel.Size = UDim2.new(1, 0, 1, 0)
    VersionLabel.Font = Enum.Font.GothamBold
    VersionLabel.Text = "v6.0"
    VersionLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    VersionLabel.TextSize = 10
    
    -- Boutons modernes (Minimize, Close)
    local ButtonContainer = Instance.new("Frame")
    ButtonContainer.Parent = TopBar
    ButtonContainer.BackgroundTransparency = 1
    ButtonContainer.Position = UDim2.new(1, -100, 0.5, -15)
    ButtonContainer.Size = UDim2.new(0, 90, 0, 30)
    
    local ButtonLayout = Instance.new("UIListLayout")
    ButtonLayout.Parent = ButtonContainer
    ButtonLayout.FillDirection = Enum.FillDirection.Horizontal
    ButtonLayout.HorizontalAlignment = Enum.HorizontalAlignment.Right
    ButtonLayout.Padding = UDim.new(0, 8)
    
    -- Minimize Button
    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.Name = "MinimizeBtn"
    MinimizeBtn.Parent = ButtonContainer
    MinimizeBtn.BackgroundColor3 = Theme.SurfaceLight
    MinimizeBtn.BorderSizePixel = 0
    MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
    MinimizeBtn.Font = Enum.Font.GothamBold
    MinimizeBtn.Text = "−"
    MinimizeBtn.TextColor3 = Theme.Text
    MinimizeBtn.TextSize = 18
    MinimizeBtn.AutoButtonColor = false
    
    local MinCorner = Instance.new("UICorner")
    MinCorner.CornerRadius = UDim.new(0, 8)
    MinCorner.Parent = MinimizeBtn
    
    -- Close Button
    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Name = "CloseBtn"
    CloseBtn.Parent = ButtonContainer
    CloseBtn.BackgroundColor3 = Color3.fromRGB(239, 68, 68)
    CloseBtn.BorderSizePixel = 0
    CloseBtn.Size = UDim2.new(0, 30, 0, 30)
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.Text = "×"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseBtn.TextSize = 20
    CloseBtn.AutoButtonColor = false
    
    local CloseCorner = Instance.new("UICorner")
    CloseCorner.CornerRadius = UDim.new(0, 8)
    CloseCorner.Parent = CloseBtn
    
    -- Container principal avec onglets
    local ContentWrapper = Instance.new("Frame")
    ContentWrapper.Parent = MainFrame
    ContentWrapper.BackgroundTransparency = 1
    ContentWrapper.Position = UDim2.new(0, 0, 0, 60)
    ContentWrapper.Size = UDim2.new(1, 0, 1, -60)
    
    -- Sidebar moderne pour les onglets
    local Sidebar = Instance.new("ScrollingFrame")
    Sidebar.Name = "Sidebar"
    Sidebar.Parent = ContentWrapper
    Sidebar.BackgroundColor3 = Theme.Surface
    Sidebar.BackgroundTransparency = 0.5
    Sidebar.BorderSizePixel = 0
    Sidebar.Position = UDim2.new(0, 10, 0, 10)
    Sidebar.Size = UDim2.new(0, 150, 1, -20)
    Sidebar.CanvasSize = UDim2.new(0, 0, 0, 0)
    Sidebar.ScrollBarThickness = 4
    Sidebar.ScrollBarImageColor3 = Theme.Primary
    Sidebar.ScrollBarImageTransparency = 0.5
    
    local SidebarCorner = Instance.new("UICorner")
    SidebarCorner.CornerRadius = UDim.new(0, 12)
    SidebarCorner.Parent = Sidebar
    
    local TabLayout = Instance.new("UIListLayout")
    TabLayout.Parent = Sidebar
    TabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabLayout.Padding = UDim.new(0, 6)
    
    local TabPadding = Instance.new("UIPadding")
    TabPadding.Parent = Sidebar
    TabPadding.PaddingTop = UDim.new(0, 12)
    TabPadding.PaddingLeft = UDim.new(0, 8)
    TabPadding.PaddingRight = UDim.new(0, 8)
    TabPadding.PaddingBottom = UDim.new(0, 12)
    
    -- Zone de contenu
    local ContentContainer = Instance.new("Frame")
    ContentContainer.Name = "ContentContainer"
    ContentContainer.Parent = ContentWrapper
    ContentContainer.BackgroundColor3 = Theme.Surface
    ContentContainer.BackgroundTransparency = 0.5
    ContentContainer.BorderSizePixel = 0
    ContentContainer.Position = UDim2.new(0, 170, 0, 10)
    ContentContainer.Size = UDim2.new(1, -180, 1, -20)
    
    local ContentCorner = Instance.new("UICorner")
    ContentCorner.CornerRadius = UDim.new(0, 12)
    ContentCorner.Parent = ContentContainer
    
    -- Fonctionnalités Minimize/Close
    local minimized = false
    local originalSize = MainFrame.Size
    
    MinimizeBtn.MouseButton1Click:Connect(function()
        minimized = not minimized
        if minimized then
            Spring(MainFrame, {Size = UDim2.new(0, 600, 0, 60)}, 0.4)
            MinimizeBtn.Text = "□"
            Tween(BlurEffect, {Size = 0}, 0.3)
        else
            Spring(MainFrame, {Size = originalSize}, 0.4)
            MinimizeBtn.Text = "−"
            Tween(BlurEffect, {Size = 10}, 0.3)
        end
    end)
    
    CloseBtn.MouseButton1Click:Connect(function()
        Spring(MainFrame, {Size = UDim2.new(0, 0, 0, 0)}, 0.4)
        Tween(BlurEffect, {Size = 0}, 0.3)
        wait(0.4)
        ScreenGui:Destroy()
        BlurEffect:Destroy()
    end)
    
    -- Effets hover
    MinimizeBtn.MouseEnter:Connect(function()
        Tween(MinimizeBtn, {BackgroundColor3 = Theme.Hover}, 0.2)
        Spring(MinimizeBtn, {Size = UDim2.new(0, 32, 0, 32)}, 0.2)
    end)
    
    MinimizeBtn.MouseLeave:Connect(function()
        Tween(MinimizeBtn, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
        Spring(MinimizeBtn, {Size = UDim2.new(0, 30, 0, 30)}, 0.2)
    end)
    
    CloseBtn.MouseEnter:Connect(function()
        Tween(CloseBtn, {BackgroundColor3 = Color3.fromRGB(248, 113, 113)}, 0.2)
        Spring(CloseBtn, {Size = UDim2.new(0, 32, 0, 32)}, 0.2)
    end)
    
    CloseBtn.MouseLeave:Connect(function()
        Tween(CloseBtn, {BackgroundColor3 = Color3.fromRGB(239, 68, 68)}, 0.2)
        Spring(CloseBtn, {Size = UDim2.new(0, 30, 0, 30)}, 0.2)
    end)
    
    -- Dragging
    MakeDraggable(MainFrame, TopBar)
    
    -- Fonction CreateTab
    function WindowManager:CreateTab(name, icon)
        local TabManager = {}
        TabManager.Elements = {}
        
        -- Bouton d'onglet moderne
        local TabButton = Instance.new("TextButton")
        TabButton.Name = name
        TabButton.Parent = Sidebar
        TabButton.BackgroundColor3 = Theme.SurfaceLight
        TabButton.BorderSizePixel = 0
        TabButton.Size = UDim2.new(1, 0, 0, 38)
        TabButton.Font = Enum.Font.GothamSemibold
        TabButton.Text = "  " .. name
        TabButton.TextColor3 = Theme.TextSecondary
        TabButton.TextSize = 13
        TabButton.TextXAlignment = Enum.TextXAlignment.Left
        TabButton.AutoButtonColor = false
        
        local TabCorner = Instance.new("UICorner")
        TabCorner.CornerRadius = UDim.new(0, 10)
        TabCorner.Parent = TabButton
        
        -- Indicateur actif
        local ActiveIndicator = Instance.new("Frame")
        ActiveIndicator.Name = "ActiveIndicator"
        ActiveIndicator.Parent = TabButton
        ActiveIndicator.BackgroundColor3 = Theme.Primary
        ActiveIndicator.BorderSizePixel = 0
        ActiveIndicator.Position = UDim2.new(0, 0, 0, 0)
        ActiveIndicator.Size = UDim2.new(0, 0, 1, 0)
        
        local IndicatorCorner = Instance.new("UICorner")
        IndicatorCorner.CornerRadius = UDim.new(0, 10)
        IndicatorCorner.Parent = ActiveIndicator
        
        -- Contenu de l'onglet
        local TabContent = Instance.new("ScrollingFrame")
        TabContent.Name = name .. "Content"
        TabContent.Parent = ContentContainer
        TabContent.BackgroundTransparency = 1
        TabContent.BorderSizePixel = 0
        TabContent.Size = UDim2.new(1, 0, 1, 0)
        TabContent.CanvasSize = UDim2.new(0, 0, 0, 0)
        TabContent.ScrollBarThickness = 4
        TabContent.ScrollBarImageColor3 = Theme.Primary
        TabContent.ScrollBarImageTransparency = 0.5
        TabContent.Visible = false
        
        local ContentLayout = Instance.new("UIListLayout")
        ContentLayout.Parent = TabContent
        ContentLayout.SortOrder = Enum.SortOrder.LayoutOrder
        ContentLayout.Padding = UDim.new(0, 10)
        
        ContentLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            TabContent.CanvasSize = UDim2.new(0, 0, 0, ContentLayout.AbsoluteContentSize.Y + 30)
        end)
        
        local ContentPadding = Instance.new("UIPadding")
        ContentPadding.Parent = TabContent
        ContentPadding.PaddingTop = UDim.new(0, 15)
        ContentPadding.PaddingLeft = UDim.new(0, 15)
        ContentPadding.PaddingRight = UDim.new(0, 15)
        ContentPadding.PaddingBottom = UDim.new(0, 15)
        
        -- Sélection d'onglet
        TabButton.MouseButton1Click:Connect(function()
            for _, tab in pairs(WindowManager.Tabs) do
                Tween(tab.Button, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
                Tween(tab.Button, {TextColor3 = Theme.TextSecondary}, 0.2)
                Tween(tab.Button:FindFirstChild("ActiveIndicator"), {Size = UDim2.new(0, 0, 1, 0)}, 0.3)
                tab.Content.Visible = false
            end
            
            Tween(TabButton, {BackgroundColor3 = Theme.Hover}, 0.2)
            Tween(TabButton, {TextColor3 = Theme.Primary}, 0.2)
            Spring(ActiveIndicator, {Size = UDim2.new(0, 3, 1, 0)}, 0.3)
            TabContent.Visible = true
            WindowManager.CurrentTab = TabManager
        end)
        
        -- Hover
        TabButton.MouseEnter:Connect(function()
            if TabButton.TextColor3 ~= Theme.Primary then
                Tween(TabButton, {BackgroundColor3 = Theme.Hover}, 0.2)
            end
        end)
        
        TabButton.MouseLeave:Connect(function()
            if TabButton.TextColor3 ~= Theme.Primary then
                Tween(TabButton, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end
        end)
        
        table.insert(WindowManager.Tabs, {Button = TabButton, Content = TabContent, Manager = TabManager})
        
        -- Auto-select premier onglet
        if #WindowManager.Tabs == 1 then
            Tween(TabButton, {BackgroundColor3 = Theme.Hover}, 0.2)
            Tween(TabButton, {TextColor3 = Theme.Primary}, 0.2)
            Spring(ActiveIndicator, {Size = UDim2.new(0, 3, 1, 0)}, 0.3)
            TabContent.Visible = true
            WindowManager.CurrentTab = TabManager
        end
        
        -- ÉLÉMENTS UI MODERNES
        
        -- Add Button
        function TabManager:AddButton(text, callback)
            local Button = Instance.new("TextButton")
            Button.Name = text
            Button.Parent = TabContent
            Button.BackgroundColor3 = Theme.SurfaceLight
            Button.BorderSizePixel = 0
            Button.Size = UDim2.new(1, 0, 0, 44)
            Button.Font = Enum.Font.GothamSemibold
            Button.Text = text
            Button.TextColor3 = Theme.Text
            Button.TextSize = 14
            Button.AutoButtonColor = false
            
            local BtnCorner = Instance.new("UICorner")
            BtnCorner.CornerRadius = UDim.new(0, 10)
            BtnCorner.Parent = Button
            
            local BtnGradient = Instance.new("UIGradient")
            BtnGradient.Parent = Button
            BtnGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Theme.SurfaceLight),
                ColorSequenceKeypoint.new(1, Theme.Hover)
            }
            BtnGradient.Rotation = 90
            
            Button.MouseButton1Click:Connect(function()
                pcall(callback)
                Spring(Button, {Size = UDim2.new(1, 0, 0, 48)}, 0.1)
                Tween(Button, {BackgroundColor3 = Theme.Primary}, 0.1)
                wait(0.15)
                Spring(Button, {Size = UDim2.new(1, 0, 0, 44)}, 0.1)
                Tween(Button, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            Button.MouseEnter:Connect(function()
                Tween(Button, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            Button.MouseLeave:Connect(function()
                Tween(Button, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return Button
        end
        
        -- Add Toggle (moderne)
        function TabManager:AddToggle(text, default, callback)
            local toggled = default or false
            
            local ToggleFrame = Instance.new("Frame")
            ToggleFrame.Name = text
            ToggleFrame.Parent = TabContent
            ToggleFrame.BackgroundColor3 = Theme.SurfaceLight
            ToggleFrame.BorderSizePixel = 0
            ToggleFrame.Size = UDim2.new(1, 0, 0, 44)
            
            local ToggleCorner = Instance.new("UICorner")
            ToggleCorner.CornerRadius = UDim.new(0, 10)
            ToggleCorner.Parent = ToggleFrame
            
            local Label = Instance.new("TextLabel")
            Label.Parent = ToggleFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 15, 0, 0)
            Label.Size = UDim2.new(1, -80, 1, 0)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local ToggleButton = Instance.new("TextButton")
            ToggleButton.Parent = ToggleFrame
            ToggleButton.BackgroundColor3 = toggled and Theme.Primary or Theme.Border
            ToggleButton.BorderSizePixel = 0
            ToggleButton.Position = UDim2.new(1, -55, 0.5, -12)
            ToggleButton.Size = UDim2.new(0, 48, 0, 24)
            ToggleButton.Text = ""
            ToggleButton.AutoButtonColor = false
            
            local ToggleBtnCorner = Instance.new("UICorner")
            ToggleBtnCorner.CornerRadius = UDim.new(1, 0)
            ToggleBtnCorner.Parent = ToggleButton
            
            local ToggleCircle = Instance.new("Frame")
            ToggleCircle.Parent = ToggleButton
            ToggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            ToggleCircle.BorderSizePixel = 0
            ToggleCircle.Position = toggled and UDim2.new(1, -20, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)
            ToggleCircle.Size = UDim2.new(0, 20, 0, 20)
            
            local CircleCorner = Instance.new("UICorner")
            CircleCorner.CornerRadius = UDim.new(1, 0)
            CircleCorner.Parent = ToggleCircle
            
            -- Ombre du cercle
            local CircleShadow = Instance.new("ImageLabel")
            CircleShadow.Parent = ToggleCircle
            CircleShadow.BackgroundTransparency = 1
            CircleShadow.Position = UDim2.new(0, -4, 0, -4)
            CircleShadow.Size = UDim2.new(1, 8, 1, 8)
            CircleShadow.ZIndex = 0
            CircleShadow.Image = "rbxassetid://5028857084"
            CircleShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
            CircleShadow.ImageTransparency = 0.7
            
            ToggleButton.MouseButton1Click:Connect(function()
                toggled = not toggled
                
                Spring(ToggleButton, {BackgroundColor3 = toggled and Theme.Primary or Theme.Border}, 0.3)
                Spring(ToggleCircle, {Position = toggled and UDim2.new(1, -20, 0.5, -10) or UDim2.new(0, 2, 0.5, -10)}, 0.3)
                
                pcall(callback, toggled)
            end)
            
            ToggleFrame.MouseEnter:Connect(function()
                Tween(ToggleFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            ToggleFrame.MouseLeave:Connect(function()
                Tween(ToggleFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return ToggleFrame
        end
        
        -- Add Slider (moderne avec gradient)
        function TabManager:AddSlider(text, min, max, default, callback)
            local value = default or min
            
            local SliderFrame = Instance.new("Frame")
            SliderFrame.Name = text
            SliderFrame.Parent = TabContent
            SliderFrame.BackgroundColor3 = Theme.SurfaceLight
            SliderFrame.BorderSizePixel = 0
            SliderFrame.Size = UDim2.new(1, 0, 0, 60)
            
            local SliderCorner = Instance.new("UICorner")
            SliderCorner.CornerRadius = UDim.new(0, 10)
            SliderCorner.Parent = SliderFrame
            
            local Label = Instance.new("TextLabel")
            Label.Parent = SliderFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 15, 0, 8)
            Label.Size = UDim2.new(1, -80, 0, 18)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local ValueLabel = Instance.new("TextLabel")
            ValueLabel.Parent = SliderFrame
            ValueLabel.BackgroundTransparency = 1
            ValueLabel.Position = UDim2.new(1, -65, 0, 8)
            ValueLabel.Size = UDim2.new(0, 50, 0, 18)
            ValueLabel.Font = Enum.Font.GothamBold
            ValueLabel.Text = tostring(value)
            ValueLabel.TextColor3 = Theme.Primary
            ValueLabel.TextSize = 14
            ValueLabel.TextXAlignment = Enum.TextXAlignment.Right
            
            local SliderBar = Instance.new("Frame")
            SliderBar.Parent = SliderFrame
            SliderBar.BackgroundColor3 = Theme.Border
            SliderBar.BorderSizePixel = 0
            SliderBar.Position = UDim2.new(0, 15, 1, -20)
            SliderBar.Size = UDim2.new(1, -30, 0, 8)
            
            local BarCorner = Instance.new("UICorner")
            BarCorner.CornerRadius = UDim.new(1, 0)
            BarCorner.Parent = SliderBar
            
            local SliderFill = Instance.new("Frame")
            SliderFill.Parent = SliderBar
            SliderFill.BackgroundColor3 = Theme.Primary
            SliderFill.BorderSizePixel = 0
            SliderFill.Size = UDim2.new((value - min) / (max - min), 0, 1, 0)
            
            local FillCorner = Instance.new("UICorner")
            FillCorner.CornerRadius = UDim.new(1, 0)
            FillCorner.Parent = SliderFill
            
            -- Gradient animé
            local FillGradient = Instance.new("UIGradient")
            FillGradient.Parent = SliderFill
            FillGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Theme.Primary),
                ColorSequenceKeypoint.new(0.5, Theme.Secondary),
                ColorSequenceKeypoint.new(1, Theme.Accent)
            }
            
            local SliderButton = Instance.new("Frame")
            SliderButton.Parent = SliderBar
            SliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            SliderButton.BorderSizePixel = 0
            SliderButton.Position = UDim2.new((value - min) / (max - min), -10, 0.5, -10)
            SliderButton.Size = UDim2.new(0, 20, 0, 20)
            
            local BtnCorner = Instance.new("UICorner")
            BtnCorner.CornerRadius = UDim.new(1, 0)
            BtnCorner.Parent = SliderButton
            
            -- Effet glow sur le bouton
            local BtnGlow = CreateGlow(SliderButton, UDim2.new(2, 0, 2, 0), Theme.Primary)
            
            local SliderInput = Instance.new("TextButton")
            SliderInput.Parent = SliderBar
            SliderInput.BackgroundTransparency = 1
            SliderInput.Size = UDim2.new(1, 0, 1, 0)
            SliderInput.Text = ""
            
            local dragging = false
            
            SliderInput.MouseButton1Down:Connect(function()
                dragging = true
                Spring(SliderButton, {Size = UDim2.new(0, 24, 0, 24)}, 0.2)
            end)
            
            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 then
                    dragging = false
                    Spring(SliderButton, {Size = UDim2.new(0, 20, 0, 20)}, 0.2)
                end
            end)
            
            UserInputService.InputChanged:Connect(function(input)
                if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
                    local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
                    value = math.floor(min + (max - min) * pos)
                    
                    SliderFill.Size = UDim2.new(pos, 0, 1, 0)
                    SliderButton.Position = UDim2.new(pos, -10, 0.5, -10)
                    ValueLabel.Text = tostring(value)
                    
                    pcall(callback, value)
                end
            end)
            
            SliderFrame.MouseEnter:Connect(function()
                Tween(SliderFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            SliderFrame.MouseLeave:Connect(function()
                Tween(SliderFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return SliderFrame
        end
        
        -- Add Dropdown (moderne)
        function TabManager:AddDropdown(text, options, callback)
            local selected = options[1] or "None"
            local opened = false
            
            local DropdownFrame = Instance.new("Frame")
            DropdownFrame.Name = text
            DropdownFrame.Parent = TabContent
            DropdownFrame.BackgroundColor3 = Theme.SurfaceLight
            DropdownFrame.BorderSizePixel = 0
            DropdownFrame.Size = UDim2.new(1, 0, 0, 44)
            DropdownFrame.ClipsDescendants = true
            
            local DropCorner = Instance.new("UICorner")
            DropCorner.CornerRadius = UDim.new(0, 10)
            DropCorner.Parent = DropdownFrame
            
            local Label = Instance.new("TextLabel")
            Label.Parent = DropdownFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 15, 0, 0)
            Label.Size = UDim2.new(0.5, -20, 0, 44)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local DropButton = Instance.new("TextButton")
            DropButton.Parent = DropdownFrame
            DropButton.BackgroundColor3 = Theme.Hover
            DropButton.BorderSizePixel = 0
            DropButton.Position = UDim2.new(0.5, 5, 0, 7)
            DropButton.Size = UDim2.new(0.5, -20, 0, 30)
            DropButton.Font = Enum.Font.Gotham
            DropButton.Text = selected
            DropButton.TextColor3 = Theme.Text
            DropButton.TextSize = 12
            DropButton.AutoButtonColor = false
            
            local DropBtnCorner = Instance.new("UICorner")
            DropBtnCorner.CornerRadius = UDim.new(0, 8)
            DropBtnCorner.Parent = DropButton
            
            local ChevronIcon = Instance.new("TextLabel")
            ChevronIcon.Parent = DropButton
            ChevronIcon.BackgroundTransparency = 1
            ChevronIcon.Position = UDim2.new(1, -20, 0, 0)
            ChevronIcon.Size = UDim2.new(0, 20, 1, 0)
            ChevronIcon.Font = Enum.Font.GothamBold
            ChevronIcon.Text = "▼"
            ChevronIcon.TextColor3 = Theme.Primary
            ChevronIcon.TextSize = 10
            
            local OptionsFrame = Instance.new("Frame")
            OptionsFrame.Parent = DropdownFrame
            OptionsFrame.BackgroundTransparency = 1
            OptionsFrame.Position = UDim2.new(0, 0, 0, 44)
            OptionsFrame.Size = UDim2.new(1, 0, 0, #options * 36)
            
            local OptionsList = Instance.new("UIListLayout")
            OptionsList.Parent = OptionsFrame
            OptionsList.SortOrder = Enum.SortOrder.LayoutOrder
            OptionsList.Padding = UDim.new(0, 4)
            
            local OptionsPadding = Instance.new("UIPadding")
            OptionsPadding.Parent = OptionsFrame
            OptionsPadding.PaddingLeft = UDim.new(0, 8)
            OptionsPadding.PaddingRight = UDim.new(0, 8)
            OptionsPadding.PaddingTop = UDim.new(0, 8)
            
            for _, option in ipairs(options) do
                local OptionBtn = Instance.new("TextButton")
                OptionBtn.Parent = OptionsFrame
                OptionBtn.BackgroundColor3 = Theme.Hover
                OptionBtn.BorderSizePixel = 0
                OptionBtn.Size = UDim2.new(1, 0, 0, 32)
                OptionBtn.Font = Enum.Font.Gotham
                OptionBtn.Text = option
                OptionBtn.TextColor3 = Theme.TextSecondary
                OptionBtn.TextSize = 12
                OptionBtn.AutoButtonColor = false
                
                local OptCorner = Instance.new("UICorner")
                OptCorner.CornerRadius = UDim.new(0, 8)
                OptCorner.Parent = OptionBtn
                
                OptionBtn.MouseButton1Click:Connect(function()
                    selected = option
                    DropButton.Text = selected
                    opened = false
                    Spring(DropdownFrame, {Size = UDim2.new(1, 0, 0, 44)}, 0.3)
                    Tween(ChevronIcon, {Rotation = 0}, 0.3)
                    pcall(callback, selected)
                end)
                
                OptionBtn.MouseEnter:Connect(function()
                    Tween(OptionBtn, {BackgroundColor3 = Theme.Primary}, 0.2)
                    Tween(OptionBtn, {TextColor3 = Color3.fromRGB(255, 255, 255)}, 0.2)
                end)
                
                OptionBtn.MouseLeave:Connect(function()
                    Tween(OptionBtn, {BackgroundColor3 = Theme.Hover}, 0.2)
                    Tween(OptionBtn, {TextColor3 = Theme.TextSecondary}, 0.2)
                end)
            end
            
            DropButton.MouseButton1Click:Connect(function()
                opened = not opened
                if opened then
                    Spring(DropdownFrame, {Size = UDim2.new(1, 0, 0, 44 + #options * 36 + 12)}, 0.3)
                    Tween(ChevronIcon, {Rotation = 180}, 0.3)
                else
                    Spring(DropdownFrame, {Size = UDim2.new(1, 0, 0, 44)}, 0.3)
                    Tween(ChevronIcon, {Rotation = 0}, 0.3)
                end
            end)
            
            DropdownFrame.MouseEnter:Connect(function()
                if not opened then
                    Tween(DropdownFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
                end
            end)
            
            DropdownFrame.MouseLeave:Connect(function()
                if not opened then
                    Tween(DropdownFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
                end
            end)
            
            return DropdownFrame
        end
        
        -- Add Label (moderne)
        function TabManager:AddLabel(text)
            local Label = Instance.new("TextLabel")
            Label.Parent = TabContent
            Label.BackgroundTransparency = 1
            Label.Size = UDim2.new(1, 0, 0, 30)
            Label.Font = Enum.Font.GothamBold
            Label.Text = text
            Label.TextColor3 = Theme.Primary
            Label.TextSize = 16
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local LabelGradient = Instance.new("UIGradient")
            LabelGradient.Parent = Label
            LabelGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Theme.Primary),
                ColorSequenceKeypoint.new(1, Theme.Secondary)
            }
            
            return Label
        end
        
        -- Add Paragraph (carte moderne)
        function TabManager:AddParagraph(title, content)
            local ParagraphFrame = Instance.new("Frame")
            ParagraphFrame.Name = title
            ParagraphFrame.Parent = TabContent
            ParagraphFrame.BackgroundColor3 = Theme.SurfaceLight
            ParagraphFrame.BorderSizePixel = 0
            ParagraphFrame.Size = UDim2.new(1, 0, 0, 90)
            
            local ParaCorner = Instance.new("UICorner")
            ParaCorner.CornerRadius = UDim.new(0, 10)
            ParaCorner.Parent = ParagraphFrame
            
            -- Accent coloré sur le côté
            local AccentBar = Instance.new("Frame")
            AccentBar.Parent = ParagraphFrame
            AccentBar.BackgroundColor3 = Theme.Primary
            AccentBar.BorderSizePixel = 0
            AccentBar.Size = UDim2.new(0, 4, 1, 0)
            
            local AccentCorner = Instance.new("UICorner")
            AccentCorner.CornerRadius = UDim.new(0, 10)
            AccentCorner.Parent = AccentBar
            
            local AccentGradient = Instance.new("UIGradient")
            AccentGradient.Parent = AccentBar
            AccentGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Theme.Primary),
                ColorSequenceKeypoint.new(1, Theme.Secondary)
            }
            AccentGradient.Rotation = 90
            
            local Title = Instance.new("TextLabel")
            Title.Parent = ParagraphFrame
            Title.BackgroundTransparency = 1
            Title.Position = UDim2.new(0, 18, 0, 12)
            Title.Size = UDim2.new(1, -36, 0, 22)
            Title.Font = Enum.Font.GothamBold
            Title.Text = title
            Title.TextColor3 = Theme.Text
            Title.TextSize = 15
            Title.TextXAlignment = Enum.TextXAlignment.Left
            
            local Content = Instance.new("TextLabel")
            Content.Parent = ParagraphFrame
            Content.BackgroundTransparency = 1
            Content.Position = UDim2.new(0, 18, 0, 38)
            Content.Size = UDim2.new(1, -36, 1, -50)
            Content.Font = Enum.Font.Gotham
            Content.Text = content
            Content.TextColor3 = Theme.TextSecondary
            Content.TextSize = 12
            Content.TextXAlignment = Enum.TextXAlignment.Left
            Content.TextYAlignment = Enum.TextYAlignment.Top
            Content.TextWrapped = true
            
            ParagraphFrame.MouseEnter:Connect(function()
                Tween(ParagraphFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            ParagraphFrame.MouseLeave:Connect(function()
                Tween(ParagraphFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return ParagraphFrame
        end
        
        -- Add Input (moderne)
        function TabManager:AddInput(text, placeholder, callback)
            local InputFrame = Instance.new("Frame")
            InputFrame.Name = text
            InputFrame.Parent = TabContent
            InputFrame.BackgroundColor3 = Theme.SurfaceLight
            InputFrame.BorderSizePixel = 0
            InputFrame.Size = UDim2.new(1, 0, 0, 75)
            
            local InputCorner = Instance.new("UICorner")
            InputCorner.CornerRadius = UDim.new(0, 10)
            InputCorner.Parent = InputFrame
            
            local Label = Instance.new("TextLabel")
            Label.Parent = InputFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 15, 0, 12)
            Label.Size = UDim2.new(1, -30, 0, 20)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local InputBox = Instance.new("TextBox")
            InputBox.Parent = InputFrame
            InputBox.BackgroundColor3 = Theme.Background
            InputBox.BorderSizePixel = 0
            InputBox.Position = UDim2.new(0, 15, 0, 40)
            InputBox.Size = UDim2.new(1, -30, 0, 28)
            InputBox.Font = Enum.Font.Gotham
            InputBox.PlaceholderText = placeholder or "Enter text..."
            InputBox.PlaceholderColor3 = Theme.TextMuted
            InputBox.Text = ""
            InputBox.TextColor3 = Theme.Text
            InputBox.TextSize = 13
            InputBox.TextXAlignment = Enum.TextXAlignment.Left
            InputBox.ClearTextOnFocus = false
            
            local BoxCorner = Instance.new("UICorner")
            BoxCorner.CornerRadius = UDim.new(0, 8)
            BoxCorner.Parent = InputBox
            
            local BoxStroke = Instance.new("UIStroke")
            BoxStroke.Parent = InputBox
            BoxStroke.Color = Theme.Border
            BoxStroke.Thickness = 1
            BoxStroke.Transparency = 0.5
            
            local BoxPadding = Instance.new("UIPadding")
            BoxPadding.Parent = InputBox
            BoxPadding.PaddingLeft = UDim.new(0, 12)
            BoxPadding.PaddingRight = UDim.new(0, 12)
            
            InputBox.Focused:Connect(function()
                Tween(BoxStroke, {Color = Theme.Primary, Thickness = 2, Transparency = 0}, 0.2)
            end)
            
            InputBox.FocusLost:Connect(function(enterPressed)
                Tween(BoxStroke, {Color = Theme.Border, Thickness = 1, Transparency = 0.5}, 0.2)
                if enterPressed then
                    pcall(callback, InputBox.Text)
                end
            end)
            
            InputFrame.MouseEnter:Connect(function()
                Tween(InputFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            InputFrame.MouseLeave:Connect(function()
                Tween(InputFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return InputFrame
        end
        
        -- Add Keybind (ultra moderne)
        function TabManager:AddKeybind(text, defaultKey, callback)
            local currentKey = defaultKey or Enum.KeyCode.E
            local waitingForKey = false
            
            local KeybindFrame = Instance.new("Frame")
            KeybindFrame.Name = text
            KeybindFrame.Parent = TabContent
            KeybindFrame.BackgroundColor3 = Theme.SurfaceLight
            KeybindFrame.BorderSizePixel = 0
            KeybindFrame.Size = UDim2.new(1, 0, 0, 44)
            
            local KeyCorner = Instance.new("UICorner")
            KeyCorner.CornerRadius = UDim.new(0, 10)
            KeyCorner.Parent = KeybindFrame
            
            local Label = Instance.new("TextLabel")
            Label.Parent = KeybindFrame
            Label.BackgroundTransparency = 1
            Label.Position = UDim2.new(0, 15, 0, 0)
            Label.Size = UDim2.new(0.6, 0, 1, 0)
            Label.Font = Enum.Font.GothamSemibold
            Label.Text = text
            Label.TextColor3 = Theme.Text
            Label.TextSize = 14
            Label.TextXAlignment = Enum.TextXAlignment.Left
            
            local KeyButton = Instance.new("TextButton")
            KeyButton.Parent = KeybindFrame
            KeyButton.BackgroundColor3 = Theme.Primary
            KeyButton.BorderSizePixel = 0
            KeyButton.Position = UDim2.new(1, -105, 0.5, -16)
            KeyButton.Size = UDim2.new(0, 95, 0, 32)
            KeyButton.Font = Enum.Font.GothamBold
            KeyButton.Text = currentKey.Name
            KeyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
            KeyButton.TextSize = 13
            KeyButton.AutoButtonColor = false
            
            local KeyBtnCorner = Instance.new("UICorner")
            KeyBtnCorner.CornerRadius = UDim.new(0, 8)
            KeyBtnCorner.Parent = KeyButton
            
            local KeyGradient = Instance.new("UIGradient")
            KeyGradient.Parent = KeyButton
            KeyGradient.Color = ColorSequence.new{
                ColorSequenceKeypoint.new(0, Theme.Primary),
                ColorSequenceKeypoint.new(1, Theme.Secondary)
            }
            KeyGradient.Rotation = 45
            
            KeyButton.MouseButton1Click:Connect(function()
                waitingForKey = true
                KeyButton.Text = "Press a key..."
                Spring(KeyButton, {Size = UDim2.new(0, 100, 0, 36)}, 0.2)
            end)
            
            UserInputService.InputBegan:Connect(function(input, gameProcessed)
                if waitingForKey and input.UserInputType == Enum.UserInputType.Keyboard then
                    currentKey = input.KeyCode
                    KeyButton.Text = currentKey.Name
                    waitingForKey = false
                    Spring(KeyButton, {Size = UDim2.new(0, 95, 0, 32)}, 0.2)
                end
                
                if not gameProcessed and input.KeyCode == currentKey then
                    pcall(callback)
                    -- Effet visuel rapide
                    Tween(KeyButton, {BackgroundColor3 = Theme.Success}, 0.1)
                    wait(0.1)
                    Tween(KeyButton, {BackgroundColor3 = Theme.Primary}, 0.2)
                end
            end)
            
            KeybindFrame.MouseEnter:Connect(function()
                Tween(KeybindFrame, {BackgroundColor3 = Theme.Hover}, 0.2)
            end)
            
            KeybindFrame.MouseLeave:Connect(function()
                Tween(KeybindFrame, {BackgroundColor3 = Theme.SurfaceLight}, 0.2)
            end)
            
            return KeybindFrame
        end
        
        return TabManager
    end
    
    return WindowManager
end

return Library

--[[
╔════════════════════════════════════════════════════════════════╗
║              WoW TEAM HUB V6.0 - ULTRA MODERN UI               ║
║                    EXEMPLE D'UTILISATION                       ║
╚════════════════════════════════════════════════════════════════╝

local Library = loadstring(game:HttpGet("URL_DU_SCRIPT"))()
local Window = Library:CreateWindow("WoW Team Hub v6.0")

-- Créer des onglets
local MainTab = Window:CreateTab("Main", "home")
local PlayerTab = Window:CreateTab("Player", "user")
local CombatTab = Window:CreateTab("Combat", "sword")
local SettingsTab = Window:CreateTab("Settings", "settings")

-- ═══════════════════════════════════════
-- ONGLET COMBAT
-- ═══════════════════════════════════════

CombatTab:AddLabel("⚔️ Paramètres de Combat")

CombatTab:AddToggle("Auto Attack", false, function(state)
    print("Auto Attack:", state)
    _G.AutoAttack = state
end)

CombatTab:AddToggle("Aimbot", false, function(state)
    print("Aimbot:", state)
    _G.Aimbot = state
end)

CombatTab:AddSlider("Attack Range", 10, 100, 20, function(value)
    _G.AttackRange = value
    print("Range:", value)
end)

CombatTab:AddDropdown("Target Priority", {"Nearest", "Lowest HP", "Highest HP", "Random"}, function(selected)
    print("Priority:", selected)
    _G.TargetPriority = selected
end)

CombatTab:AddKeybind("Toggle Combat Mode", Enum.KeyCode.C, function()
    print("Combat Mode toggled!")
end)

-- ═══════════════════════════════════════
-- ONGLET SETTINGS
-- ═══════════════════════════════════════

SettingsTab:AddLabel("⚙️ Configuration")

SettingsTab:AddParagraph("📌 À propos", "WoW Team Hub v6.0 - Interface ultra-moderne avec animations fluides et design glassmorphism. Créé avec passion!")

SettingsTab:AddToggle("Show Notifications", true, function(state)
    _G.ShowNotifications = state
end)

SettingsTab:AddDropdown("Theme", {"Purple", "Blue", "Green", "Red", "Orange"}, function(selected)
    print("Theme changed to:", selected)
    -- Vous pouvez ajouter un système de thème ici
end)

SettingsTab:AddButton("Reset Settings", function()
    print("Settings reset!")
end)

SettingsTab:AddButton("Destroy UI", function()
    game.CoreGui.WoWTeamHubV6:Destroy()
end)

SettingsTab:AddKeybind("Toggle UI", Enum.KeyCode.RightShift, function()
    local gui = game.CoreGui.WoWTeamHubV6
    gui.Enabled = not gui.Enabled
end)

--[[
╔════════════════════════════════════════════════════════════════╗
║                      FONCTIONNALITÉS V6.0                      ║
╚════════════════════════════════════════════════════════════════╝

✨ DESIGN ULTRA-MODERNE:
   • Glassmorphism & Neumorphism
   • Animations fluides et élastiques
   • Gradient animés multicolores
   • Bordures lumineuses RGB
   • Effets de profondeur (shadows & glows)

🎨 ÉLÉMENTS UI DISPONIBLES:
   • AddButton()      - Boutons interactifs
   • AddToggle()      - Switches modernes
   • AddSlider()      - Sliders avec gradients
   • AddDropdown()    - Menus déroulants animés
   • AddLabel()       - Titres de section
   • AddParagraph()   - Cartes d'information
   • AddInput()       - Champs de saisie
   • AddKeybind()     - Configuration des touches

🚀 ANIMATIONS:
   • Spring animations (effet rebond)
   • Smooth transitions
   • Hover effects
   • Click feedback
   • Color morphing

💡 OPTIMISATIONS:
   • Code ultra-optimisé
   • Pas de lag
   • Faible consommation mémoire
   • Compatible tous devices

🎯 UTILISATION:
   1. Chargez le script
   2. Créez une fenêtre avec CreateWindow()
   3. Ajoutez des onglets avec CreateTab()
   4. Ajoutez des éléments dans chaque onglet
   5. Profitez de l'interface !

═══════════════════════════════════════════════════════════════

💜 CRÉÉ PAR WOW TEAM
🌟 VERSION 6.0 - ULTRA MODERN EDITION
⚡ PROPULSÉ PAR LUCIDE ICONS & MODERN DESIGN

═══════════════════════════════════════════════════════════════
]]

--[[
╔════════════════════════════════════════════════════════════════╗
║                    EXEMPLES AVANCÉS                            ║
╚════════════════════════════════════════════════════════════════╝

-- EXEMPLE 1: Créer plusieurs onglets avec toutes les fonctionnalités
local Library = loadstring(game:HttpGet("URL"))()
local Window = Library:CreateWindow("Mon Hub Ultime")

local Tab1 = Window:CreateTab("Home")
local Tab2 = Window:CreateTab("Features")
local Tab3 = Window:CreateTab("Misc")

-- EXEMPLE 2: Toggle avec callback complexe
Tab1:AddToggle("ESP Players", false, function(enabled)
    if enabled then
        -- Activer ESP
        for _, player in pairs(game.Players:GetPlayers()) do
            if player ~= game.Players.LocalPlayer then
                -- Créer ESP box
            end
        end
    else
        -- Désactiver ESP
    end
end)

-- EXEMPLE 3: Slider pour modifications dynamiques
Tab1:AddSlider("FOV", 70, 120, 70, function(value)
    game.Workspace.Camera.FieldOfView = value
end)

-- EXEMPLE 4: Dropdown pour sélection d'équipe
Tab1:AddDropdown("Select Team", {"Red Team", "Blue Team", "Green Team"}, function(team)
    -- Changer d'équipe
    game.Players.LocalPlayer.Team = game.Teams:FindFirstChild(team)
end)

-- EXEMPLE 5: Input pour recherche de joueur
Tab2:AddInput("Find Player", "Enter username...", function(username)
    local player = game.Players:FindFirstChild(username)
    if player then
        print("Player found:", player.Name)
    else
        print("Player not found")
    end
end)

-- EXEMPLE 6: Keybind pour fonction rapide
Tab3:AddKeybind("Fly Toggle", Enum.KeyCode.F, function()
    -- Toggle fly
    _G.FlyEnabled = not _G.FlyEnabled
    print("Fly:", _G.FlyEnabled)
end)

-- EXEMPLE 7: Paragraph pour instructions
Tab3:AddParagraph("How to Use", 
    "1. Select your desired features\n" ..
    "2. Configure settings with sliders\n" ..
    "3. Use keybinds for quick access\n" ..
    "4. Enjoy your enhanced experience!"
)

-- EXEMPLE 8: Multiples boutons pour actions
Tab2:AddButton("Teleport to Spawn", function()
    game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = 
        game.Workspace.SpawnLocation.CFrame
end)

Tab2:AddButton("Full Heal", function()
    game.Players.LocalPlayer.Character.Humanoid.Health = 
        game.Players.LocalPlayer.Character.Humanoid.MaxHealth
end)

Tab2:AddButton("Reset Character", function()
    game.Players.LocalPlayer.Character.Humanoid.Health = 0
end)

-- EXEMPLE 9: Combinaison toggle + slider
local speedEnabled = false
local speedValue = 16

Tab1:AddToggle("Speed Hack", false, function(state)
    speedEnabled = state
    if state then
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = speedValue
    else
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
    end
end)

Tab1:AddSlider("Speed Amount", 16, 500, 16, function(value)
    speedValue = value
    if speedEnabled then
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
    end
end)

-- EXEMPLE 10: Système de configuration sauvegardée
local Config = {
    Speed = 16,
    JumpPower = 50,
    FOV = 70,
    Team = "Red Team"
}

Tab3:AddButton("Save Config", function()
    writefile("MyHubConfig.json", game:GetService("HttpService"):JSONEncode(Config))
    print("Configuration saved!")
end)

Tab3:AddButton("Load Config", function()
    if isfile("MyHubConfig.json") then
        Config = game:GetService("HttpService"):JSONDecode(readfile("MyHubConfig.json"))
        print("Configuration loaded!")
        -- Appliquer la config
    end
end)

═══════════════════════════════════════════════════════════════

🎨 PERSONNALISATION DES COULEURS:
   
   Pour modifier le thème, changez les valeurs dans la table Theme:
   
   Theme.Primary = Color3.fromRGB(139, 92, 246)    -- Violet
   Theme.Secondary = Color3.fromRGB(236, 72, 153)  -- Rose
   Theme.Accent = Color3.fromRGB(34, 211, 238)     -- Cyan
   
   Thèmes prédéfinis:
   • Purple Dream (par défaut)
   • Ocean Blue: Primary = RGB(59, 130, 246)
   • Forest Green: Primary = RGB(16, 185, 129)
   • Sunset Orange: Primary = RGB(251, 146, 60)
   • Blood Red: Primary = RGB(239, 68, 68)

═══════════════════════════════════════════════════════════════

⚡ ASTUCES & CONSEILS:

1. Pour une meilleure organisation, créez des labels pour séparer
   les sections dans vos onglets

2. Utilisez AddParagraph pour donner des instructions claires
   à vos utilisateurs

3. Les keybinds sont parfaits pour les fonctions qu'on utilise
   souvent (fly, noclip, esp, etc.)

4. Combinez toggles et sliders pour un contrôle précis

5. Les dropdowns sont idéaux pour les sélections avec plusieurs
   options (teams, armes, modes, etc.)

6. N'oubliez pas d'ajouter un bouton "Destroy UI" dans Settings
   pour permettre aux utilisateurs de fermer proprement

7. Testez toujours vos callbacks pour éviter les erreurs

8. Utilisez pcall() dans vos callbacks pour éviter que l'UI
   crash en cas d'erreur

═══════════════════════════════════════════════════════════════

📱 COMPATIBILITÉ:

✅ PC / Desktop
✅ Mobile (tactile optimisé)
✅ Tablette
✅ Tous les executors majeurs
✅ Synapse X, Script-Ware, KRNL, Fluxus, etc.

═══════════════════════════════════════════════════════════════

🔥 NOUVEAUTÉS V6.0:

• Design glassmorphism moderne
• Animations élastiques (spring)
• Bordures RGB animées
• Gradients multicolores
• Effets glow et shadow
• Optimisation performances
• Code restructuré et clean
• Meilleure compatibilité mobile
• Transitions ultra-smooth
• Interface responsive
• Hover effects améliorés
• Click feedback visuel
• Logo animé avec rotation
• Version badge stylisé
• Sidebar moderne
• Content cards élégantes
• Input fields avec focus effect
• Dropdown avec chevron animé
• Keybind buttons avec gradient
• Toggle switches fluides
• Sliders avec fill gradient
• Paragraph avec accent bar
• Labels avec text gradient

═══════════════════════════════════════════════════════════════

💎 WOW TEAM HUB V6.0 - LA MEILLEURE UI MODERNE POUR ROBLOX
🌟 Design professionnel de qualité premium
⚡ Performances optimales
🎨 Personnalisable à l'infini
🚀 Facile à utiliser

Merci d'utiliser WoW Team Hub! ❤️

═══════════════════════════════════════════════════════════════
]]══════════════════════════════════════
-- ONGLET MAIN
-- ═══════════════════════════════════════

MainTab:AddLabel("🎮 Actions Principales")

MainTab:AddButton("Kill All Enemies", function()
    print("Kill All activé!")
    -- Votre code ici
end)

MainTab:AddToggle("Auto Farm", false, function(state)
    print("Auto Farm:", state)
    _G.AutoFarm = state
end)

MainTab:AddParagraph("ℹ️ Information", "Cette fonction permet d'automatiser le farming. Activez-la pour gagner de l'XP automatiquement!")

-- ═══════════════════════════════════════
-- ONGLET PLAYER
-- ═══════════════════════════════════════

PlayerTab:AddLabel("👤 Modifications Joueur")

PlayerTab:AddSlider("Walk Speed", 16, 200, 16, function(value)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end)

PlayerTab:AddSlider("Jump Power", 50, 300, 50, function(value)
    game.Players.LocalPlayer.Character.Humanoid.JumpPower = value
end)

PlayerTab:AddToggle("Infinite Jump", false, function(state)
    _G.InfiniteJump = state
end)

PlayerTab:AddInput("Teleport to Player", "Username...", function(text)
    print("Téléportation vers:", text)
    -- Code de téléportation
end)
